{
  "scripts": {
    "build": "rimraf dist && rollup -c rollup.config.js",
    "start:build": "npm run build && web-dev-server --root-dir dist --app-index index.html --open --compatibility none",
    "start": "web-dev-server"
  },
  "devDependencies": {
    "@open-wc/building-rollup": "^1.10.0",
    "@web/dev-server": "^0.1.17",
    "deepmerge": "^4.2.2",
    "rimraf": "^3.0.2",
    "rollup": "^2.52.1"
  },
  "name": "slate-template",
  "version": "0.0.0",
  "description": "Webcomponent slate-template following open-wc recommendations",
  "author": "slate-template",
  "license": "MIT",
  "dependencies": {
    "@webcomponents/webcomponentsjs": "^2.5.0",
    "linkset-menu": "^0.0.2",
    "lit": "^2.0.0-rc.2",
    "lit-element": "^2.5.1",
    "lit-html": "^1.4.1"
  }
}
